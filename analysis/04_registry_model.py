"""
04_registry_model.py — Pipeline entry point for registry model (IMPL-5).
Full coverage curves are generated in IMPL-6.
"""

from registry_model import (
    collapse_to_4locus,
    get_diplotype_frequencies,
    compute_coverage,
    find_registry_size,
    get_combined_haplotype_freqs,
    SG_WEIGHTS,
)


def main():
    print("Registry model ready. Run IMPL-6 for full coverage curves.")


if __name__ == '__main__':
    main()
